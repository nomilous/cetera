#!/usr/bin/env coffee

require('nez').objective 'cetera',

    category: 'Web Application Tools'

    description: """

    effortlessness, clientside, requirejs, package, modules

    """

    #
    # secret: '∫'
    # address: 'localhost'
    # port:     10001
    # cert:     './cert/develop-cert.pem'
    # key:      './cert/develop-key.pem'
    #module: start: (context, notifier, fn) -> console.log '\n', fn.toString()
    #


    # beforeAll:  -> console.log 'beforeAll:',  arguments
    # beforeEach: -> console.log 'beforeEach:', arguments

    # messenger: (msg, next) -> 
    #     console.log msg.content 
    #     next()

    error: (error) -> console.log 'ERROR!!', error.stack

    (requirements) ->

        'moo'

